#ifndef _WIN32
[ explicit_handle ] 
#else
[ explicit_handle, enable_allocate ]
#endif
interface echo {

	ReverseIt([comm_status, fault_status] status);

	ReverseWrapped([comm_status, fault_status] status);
}
