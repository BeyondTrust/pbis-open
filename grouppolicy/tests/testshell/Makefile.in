BUILD_ROOT = @builddir@/../..
SRC_ROOT=@srcdir@/../..

SHELL= /bin/sh
VPATH= @srcdir@

subdirs= @subdirs@
top_srcdir= @top_srcdir@
srcdir = @srcdir@
prefix = @prefix@
exec_prefix = @exec_prefix@
bindir = ${exec_prefix}/bin
sbindir = ${exec_prefix}/sbin
infodir = ${prefix}/info
libdir = ${prefix}/lib
mandir = ${prefix}/man/man1

DESTDIR=@DESTDIR@
CC = @CC@
CPPFLAGS = @CPPFLAGS@
CFLAGS = $(CPPFLAGS) @CFLAGS@
LDFLAGS = @LDFLAGS@
LIBS = @LIBS@
INSTALL = @INSTALL@

CFLAGS += @GP_CORE_INCLUDE@

LIBS = @CENTUTILS_LIBS@
LDFLAGS += @CENTUTILS_LDFLAGS@

OBJS = main.o

PROGRAM = testshell

all : $(PROGRAM)

$(PROGRAM) : $(OBJS) @GP_CORE_DEP_LIBS@
	$(CC) -o $@ $(OBJS) $(LIBS)

$(OBJS) : %.o : %.c
	$(CC) -c $(CFLAGS) $< -o $@

clean :
	$(RM) $(OBJS) $(PROGRAM)

distclean: clean
	$(RM) Makefile

install: all
	@echo $(DESTDIR)
	$(top_srcdir)/mkinstalldirs $(DESTDIR)$(bindir)
	$(top_srcdir)/mkinstalldirs $(DESTDIR)$(libdir)
	$(INSTALL) $(PROGRAM) $(DESTDIR)$(sbindir)

uninstall:
	-/bin/rm -f $(DESTDIR)$(bindir)/$(PROGRAM)

