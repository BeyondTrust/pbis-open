#!/bin/sh

# if this is a local deinstall, stop the Likewise lwtask daemon
if [ -z "$PKG_INSTALL_ROOT" ]
then
   LWTASKD=`ps -e -o pid,comm | grep lwtaskd | awk '{print $1}'`
   [ ! -z "$LWTASKD" ] && kill -TERM $LWTASKD
   sleep 2
fi
