#!/bin/sh

# if this is a local deinstall, stop the Likewise Netlogon daemon
if [ -z "$PKG_INSTALL_ROOT" ]
then
   LWNETLOGOND=`ps -e -o pid,comm | grep likewise-netlogond | awk '{print $1}'`
   [ ! -z "$LWNETLOGOND" ] && kill -TERM $LWNETLOGOND
   sleep 2
fi
