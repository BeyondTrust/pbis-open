#!/bin/sh

# if this is a local deinstall, stop the Likewise Eventlog daemon
if [ -z "$PKG_INSTALL_ROOT" ]
then
   LWMGMTD=`ps -e -o pid,comm | grep lwmgmtd | awk '{print $1}'`
   [ ! -z "$LWMGMTD" ] && kill -TERM $LWMGMTD
   sleep 2
fi
