#!/bin/sh

# if this is a local deinstall, stop the Likewise IO daemon
if [ -z "$PKG_INSTALL_ROOT" ]
then
   LWIOD=`ps -e -o pid,comm | grep lwiod | awk '{print $1}'`
   [ ! -z "$LWIOD" ] && kill -TERM $LWIOD
   sleep 2
fi
