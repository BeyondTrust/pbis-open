# Basic build settings

BUILD_VENDOR="likewise"
BUILD_PREFIX="/."
BUILD_SYSCONFDIR="/etc/${BUILD_VENDOR}"
BUILD_LOCALSTATEDIR="/etc/${BUILD_VENDOR}"

# Build product set

BUILD_PRODUCTS="lwesxi"

# Extra build options

BUILD_MINIMAL="true"

# Variables to make available during building
BUILD_PRECIOUS_VARIABLES="${BUILD_PRECIOUS_VARIABLES} BUILD_MINIMAL"
