# Automatically added by dh_installinit
if [ "$1" = "purge" ] ; then
	update-rc.d likewise-open remove >/dev/null || exit $?
fi
# End automatically added section
# Automatically added by dh_makeshlibs
if [ "$1" = "remove" ]; then
	ldconfig
fi
# End automatically added section
