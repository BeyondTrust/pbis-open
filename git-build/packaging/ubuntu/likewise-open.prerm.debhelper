# Automatically added by dh_installinit
if [ -x "/etc/init.d/likewise-open" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d likewise-open stop || exit $?
	else
		/etc/init.d/likewise-open stop || exit $?
	fi
fi
# End automatically added section
