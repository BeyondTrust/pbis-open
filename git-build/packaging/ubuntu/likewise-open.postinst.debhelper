# Automatically added by dh_installinit
if [ -x "/etc/init.d/likewise-open" ]; then
	update-rc.d likewise-open defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d likewise-open start || exit $?
	else
		/etc/init.d/likewise-open start || exit $?
	fi
fi
# End automatically added section
# Automatically added by dh_makeshlibs
if [ "$1" = "configure" ]; then
	ldconfig
fi
# End automatically added section
